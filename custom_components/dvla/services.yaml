lookup:
  name: Ad-hoc DVLA lookup
  description: Perform a one-off DVLA Vehicle Enquiry lookup and return JSON.
  fields:
    reg_number:
      required: true
      selector:
        text: {}
      description: Registration to query (e.g. AB12CDE).
